# Women illustrations

This folder is the destination for the existing approved women illustration library.

For safety, the current live pipeline still reads the existing PNGs from `illustrations/` at the repository root. Move/copy those PNGs here only after the new audience split is verified.

Requirements remain the same: one clean black-and-white drawing per PNG, transparent background preferred, with no quote text or full-frame background.
